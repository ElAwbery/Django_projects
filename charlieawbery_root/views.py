# Django views source data from databases and deliver it to templates
# Each view performs a specific function and has an associated template
# A view can be represented by either a function or a class method
from django.shortcuts import render

# Create your views here.
